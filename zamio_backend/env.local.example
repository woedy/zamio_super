# Local Development Environment Variables for ZamIO Django
# Copy this to .env.local and use with docker-compose.local.yml

# Django Settings
DEBUG=True
DJANGO_SETTINGS_MODULE=core.settings

# Security
# Use a non-production key for local. Set a strong secret in production.
SECRET_KEY=dev-insecure-change-me

# Database
DATABASE_URL=postgresql://zamio_user:zamio_password@db:5432/zamio_local

# Redis
REDIS_URL=redis://redis:6379/0

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Local Development Settings
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,db,redis,zamio_app,31.97.156.207
CSRF_TRUSTED_ORIGINS=http://localhost:9001,http://127.0.0.1:9001,http://31.97.156.207:9001,http://localhost:9002,http://127.0.0.1:9002,http://31.97.156.207:9002,http://31.97.156.207:9005,http://31.97.156.207:9006,http://31.97.156.207:9007
# For non-DEBUG scenarios, list allowed origins (comma-separated) for CORS
# CORS_ALLOWED_ORIGINS=http://localhost:9002,http://127.0.0.1:9002
SECURE_SSL_REDIRECT=false
SESSION_COOKIE_SECURE=false
CSRF_COOKIE_SECURE=false
CSP_DEFAULT_SRC='self'
CSP_SCRIPT_SRC='self'
CSP_STYLE_SRC='self' 'unsafe-inline'
CSP_IMG_SRC='self' data:
CSP_FONT_SRC='self' data:
CSP_CONNECT_SRC='self' http://localhost:9001
CSP_FRAME_ANCESTORS='none'
PERMISSIONS_POLICY=camera=(), microphone=(), geolocation=()
CROSS_ORIGIN_OPENER_POLICY=same-origin
CROSS_ORIGIN_EMBEDDER_POLICY=require-corp
CROSS_ORIGIN_RESOURCE_POLICY=same-origin
BASE_URL=http://localhost:9001


# Email Configuration (file-based for local development)
EMAIL_BACKEND=django.core.mail.backends.filebased.EmailBackend
EMAIL_FILE_PATH=sent_emails

# Optional: Configure SMTP for testing
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=Zamio <your-email@gmail.com>

# AWS S3 (disabled for local development)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_ENDPOINT_URL=
# AWS_S3_FILE_OVERWRITE=False
# AWS_S3_ADDRESSING_STYLE=path
# AWS_QUERYSTRING_AUTH=False
